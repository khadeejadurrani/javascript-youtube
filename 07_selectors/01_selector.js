/*writing this here for a context ,it is the shorter version of forEach loop in arrow function
fruits.forEach(fruit => console.log(fruit)); */
const fruits = document.querySelectorAll(".fruit");
fruits.forEach(f => console.log(f.textContent) );